{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "stack build --fast",
            "type": "shell",
            "command": "stack build --fast ; stack exec site build",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "stack exec site watch",
            "type": "shell",
            "command": "stack exec site watch",
            "group": "build"
        }
    ]
}